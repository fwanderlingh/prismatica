PRISMATICA
==========

## To DO:

In the Full Text screening, the segment div, inside overviewBand compactBand, contains all the titles of the included works. But with hundreds of included articles this is not feasible. Find a smarter way to list the included files.

Also this phase, should be handled same way as the screening phase, with at least two people voting, and if both are "Include" then they go to Extraction, otherwise, if one of them is Exclude they should go in a "resolve conflict" state.
