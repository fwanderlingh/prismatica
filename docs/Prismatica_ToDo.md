PRISMATICA
==========

## To DO:

1. In the Full Text screening, the segment div, inside overviewBand compactBand, contains all the titles of the included works. But with hundreds of included articles this is not feasible. Find a smarter way to list the included files.
 

2. The Full Text this should be handled same way as the screening phase, with at least two people voting, and if both are "Include" then they go to Extraction, otherwise, if one of them is Exclude they should go in a "resolve conflict" state.

3. Keep track of per-user statitics for a project for all phases, i.e.: "Screened", "Uploaded PDF", "Full Text Reviews", "Extractions".

4. This text should updated if no duplicates are encountered: "This review is waiting for imported records before deduplication can generate candidate pairs."



